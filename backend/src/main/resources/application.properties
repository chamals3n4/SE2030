spring.application.name=backend

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/se2030?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=tharindi_K27
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# Jackson Configuration
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.default-property-inclusion=non_null

# OAuth2 JWT Configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://api.asgardeo.io/t/s3n4
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://api.asgardeo.io/t/s3n4/jwks

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.oauth2=DEBUG
logging.level.org.springframework.boot.autoconfigure.security=DEBUG
